version: "3.9"

services:
  app:
    build: .
    container_name: threejs-dev
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - "5173:5173" # Vite default port
    command: npm run dev
    environment:
      - NODE_ENV=development
